doctype html
html(lang='ja')
  head
    block head
      meta(charset='UTF-8')
      title #{title ? `${title} | ` : ''}書籍(貸し出し)管理システム
      meta(name='viewport' content='width=device-width, initial-scale=1.0')
      link(rel='stylesheet', href='/css/style.css')
  body
    header
      block header

    main
      block content

    footer
      block footer
